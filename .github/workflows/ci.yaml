name: Akshay2
on:
  workflow_dispatch:
  pull_request:  
    branches:
      - main

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "GitHub Actions is working 🎉"
